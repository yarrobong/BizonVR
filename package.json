{
  "name": "portal-shop-clone",
  "private": true,
  "scripts": {
    "build:css": "npx tailwindcss -i static_src/input.css -o static/css/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
